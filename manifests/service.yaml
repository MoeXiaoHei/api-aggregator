apiVersion: v1
kind: Service
metadata:
  name: api-aggregator
  namespace: kube-system
  labels:
    app: api-aggregator
spec:
  selector:
    app: api-aggregator
  ports:
  - port: 443
    targetPort: 8443
    name: https
  type: NodePort
